import React from 'react'

function LocationData({name, count}) {
    return (
        <div>
            <li>{name}          =>           {count}</li>
        </div>
    )
}

export default LocationData
